<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/item_category_cover"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintDimensionRatio="1:1"
        app:layout_constraintTop_toTopOf="parent"
        android:contentDescription="@string/app_name" />

    <View
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="#10000000"
        app:layout_constraintBottom_toBottomOf="@id/item_category_cover"
        app:layout_constraintEnd_toEndOf="@id/item_category_cover"
        app:layout_constraintStart_toStartOf="@id/item_category_cover"
        app:layout_constraintTop_toTopOf="@id/item_category_cover" />

    <TextView
        android:id="@+id/item_category_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:textStyle="bold"
        android:textColor="@color/white"
        android:textSize="32sp"
        app:layout_constraintBottom_toBottomOf="@id/item_category_cover"
        app:layout_constraintEnd_toEndOf="@id/item_category_cover" />

</androidx.constraintlayout.widget.ConstraintLayout>